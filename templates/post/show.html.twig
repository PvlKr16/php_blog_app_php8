{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                <h3>{{ post.title }}</h3>
            </div>
            <div class="card-body">
                <p class="text-muted">
                    Блог: <a href="{{ path('blog_show', {'id': blog.id}) }}">{{ blog.title }}</a><br>
                    Автор: <strong>{{ post.author.username }}</strong><br>
                    Создано: {{ post.createdAt|date('d.m.Y H:i') }}
                    {% if post.updatedAt > post.createdAt %}
                        <br>Обновлено: {{ post.updatedAt|date('d.m.Y H:i') }}
                    {% endif %}
                </p>

                <hr>
                <div>
                    {{ post.content|nl2br }}
                </div>

                {% if attachments is not empty %}
                    <hr>
                    <h5>📎 Прикреплённые файлы ({{ attachments|length }})</h5>
                    <div class="list-group">
                        {% for attachment in attachments %}
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <span style="font-size: 24px;">{{ attachment.icon }}</span>
                                    <a href="/uploads/attachments/{{ attachment.filename }}" target="_blank" class="ms-2">
                                        {{ attachment.originalFilename }}
                                    </a>
                                    <small class="text-muted ms-2">({{ attachment.fileSizeFormatted }})</small>
                                </div>
                                {% if app.user and app.user == post.author %}
                                    <form method="post" action="{{ path('attachment_delete', {'id': attachment.id}) }}" style="display: inline;">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Удалить файл?');">
                                            Удалить
                                        </button>
                                    </form>
                                {% endif %}
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
            <div class="card-footer">
                <a href="{{ path('blog_show', {'id': blog.id}) }}" class="btn btn-secondary">Назад к блогу</a>

                {% if app.user and app.user == post.author %}
                    <a href="{{ path('post_edit', {'id': post.id}) }}" class="btn btn-warning">Редактировать</a>

                    <form method="post" action="{{ path('post_delete', {'id': post.id}) }}" style="display: inline-block;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Вы уверены, что хотите удалить эту запись?');">
                            Удалить
                        </button>
                    </form>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}