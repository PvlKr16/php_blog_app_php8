{% extends 'base.html.twig' %}

{% block title %}{{ blog.title }}{% endblock %}

{% block body %}
<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <h1 class="card-title">{{ blog.title }}</h1>
                <p class="text-muted">
                    Автор: <strong>{{ blog.author.username }}</strong><br>
                    <br>Тема: <strong>{{ blog.category ? blog.category.name : 'Без темы' }}</strong>
                    <br>Статус: <strong>{{ blog.status == 'public' ? '🌐 Общий' : '🔒 Закрытый' }}</strong>
                    {% if blog.participants|length > 0 %}
                        <br>Участники:
                        {% for participant in blog.participants %}
                            <span class="badge bg-secondary">{{ participant.username }}</span>
                        {% endfor %}
                    {% endif %}
                    Создано: {{ blog.createdAt|date('d.m.Y H:i') }}
                    {% if blog.updatedAt > blog.createdAt %}
                        <br>Обновлено: {{ blog.updatedAt|date('d.m.Y H:i') }}
                    {% endif %}
                </p>
                <hr>
                <div class="blog-content">
                    {{ blog.content|nl2br }}
                </div>
            </div>
            <div class="card-footer">
                <a href="{{ path('blog_list') }}" class="btn btn-secondary">Назад к списку</a>

                {% if app.user and app.user == blog.author %}
                    <a href="{{ path('blog_edit', {'id': blog.id}) }}" class="btn btn-warning">Редактировать</a>

                    <form method="post" action="{{ path('blog_delete', {'id': blog.id}) }}" style="display: inline-block;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Вы уверены, что хотите удалить этот блог?');">Удалить</button>
                    </form>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
